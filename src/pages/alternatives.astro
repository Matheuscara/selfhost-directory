---
import Base from '../layouts/Base.astro';
import { getAlternatives } from '../lib/data';
const alternatives = getAlternatives();
---
<Base title="Self-hosted alternatives to paid services">
  <h1 class="text-3xl font-bold text-ink">Replace a paid service</h1>
  <p class="text-muted mt-2">Pick the service you want to replace with a self-hosted option.</p>
  <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-3 mt-6">
    {alternatives.map((a) => (
      <a href={`/alternative-to/${a.slug}`}
         class="rounded-lg border border-line bg-panel hover:bg-panel-2 p-4 transition">
        <span class="text-ink font-medium">Alternative to {a.proprietary}</span>
        <span class="block text-sm text-muted mt-1">{a.blurb}</span>
      </a>
    ))}
  </div>
</Base>
